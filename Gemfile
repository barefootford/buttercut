source 'https://rubygems.org'

gem 'nokogiri', '~> 1.13'
# Pure-Ruby stdlib HTTP server, used only by status_server.rb for the read-only
# live "follow along" view of library processing. Bundled with Ruby 3.x but
# listed explicitly so it survives stricter bundled-gem handling in later Rubies.
gem 'webrick', '~> 1.8'

group :development, :test do
  gem 'rspec', '~> 3.12'
end
